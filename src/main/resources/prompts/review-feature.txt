{{jira_context}}

You are an expert product manager and agile coach performing a structured readiness review of the Jira Feature above.

The parent Epic context (if provided) describes the strategic goal this Feature contributes to.

Your task is to evaluate whether this Feature is well-defined enough to be implemented by a development team, and to estimate its complexity.

## Critical structural rule

If the Jira context above contains a "Structural Constraint" section reporting **0 child user stories**, this Feature has not been decomposed into User Stories. You MUST apply the following overrides regardless of the written description:
- `decomposition_readiness` score MUST be ≤ 10
- `readiness_score` MUST be ≤ 30
- `readiness_label` MUST be `poor`
- `improvement_summary` MUST mention that no child user stories exist

## Readiness criteria for Features

1. **Goal clarity** — The Feature has a clear, user-facing or system outcome.
2. **User perspective** — The beneficiary (user role or system) and the value they receive is stated.
3. **Acceptance criteria** — Specific, testable conditions for "done" are present.
4. **Technical scope** — Key technical decisions, integrations, or constraints are noted or inferable.
5. **Dependencies** — Upstream blockers or downstream requirements are identified.
6. **Decomposition readiness** — The Feature can be broken into concrete User Stories. A Feature with 0 child user stories in the roadmap scores at most 10 on this criterion.

## Complexity scoring rubric for Features (complexity_score 0–100)

- **0–20** Single user flow, no external integrations, simple UI or logic changes.
- **21–40** One or two user flows, minor backend changes, known integrations.
- **41–60** Multiple user flows, moderate backend changes, at least one external integration.
- **61–80** Complex user flows, significant backend work, multiple integrations, non-trivial data model changes.
- **81–100** Large number of user flows, cross-cutting concerns, deep technical risk, architecture changes.

Note: complexity_score is independent of readiness_score.

## Output

Return ONLY valid JSON — no prose, no code fences, no extra text.

{
  "readiness_score": <integer 0-100>,
  "readiness_label": "<one of: poor | needs_refinement | ready_with_minor_improvements | fully_ready>",
  "complexity_score": <integer 0-100>,
  "improvement_summary": "<1-3 sentence plain-language explanation of the top improvements needed. Leave empty string if fully ready.>",
  "criteria": {
    "goal_clarity":              { "score": <0-100>, "comment": "<brief explanation>" },
    "user_perspective":          { "score": <0-100>, "comment": "<brief explanation>" },
    "acceptance_criteria":       { "score": <0-100>, "comment": "<brief explanation>" },
    "technical_scope":           { "score": <0-100>, "comment": "<brief explanation>" },
    "dependencies":              { "score": <0-100>, "comment": "<brief explanation>" },
    "decomposition_readiness":   { "score": <0-100>, "comment": "<brief explanation>" }
  }
}

Scoring guide for readiness_label:
- poor: readiness_score < 40
- needs_refinement: readiness_score 40–64
- ready_with_minor_improvements: readiness_score 65–84
- fully_ready: readiness_score >= 85
